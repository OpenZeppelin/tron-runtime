{
  "name": "@openzeppelin/tron-runtime",
  "version": "0.1.0",
  "description": "Shared stateless native-TRON transport primitives for OpenZeppelin TRON tooling",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OpenZeppelin/tron-runtime.git"
  },
  "homepage": "https://github.com/OpenZeppelin/tron-runtime#readme",
  "bugs": {
    "url": "https://github.com/OpenZeppelin/tron-runtime/issues"
  },
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "check:package": "npm run build && node --import tsx scripts/check-package-contents.ts",
    "check:api": "npm run build && node --import tsx scripts/check-api-surface.ts",
    "conformance": "npm run build && node --import tsx scripts/conformance.ts",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "verify": "npm run typecheck && npm run build && npm test && npm run check:package && npm run check:api",
    "prepack": "npm run build",
    "prepublishOnly": "npm run check:api"
  },
  "dependencies": {
    "ethers": "^6.14.0"
  },
  "peerDependencies": {
    "tronweb": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tronweb": "6.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
